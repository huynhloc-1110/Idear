@model IEnumerable<Idear.Models.Topic>

@{
    ViewData["Title"] = "Ideas";
}

<h1>View Topics</h1>

<table class="table table-hover" id="table_id">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClosureDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinalClosureDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ClosureDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FinalClosureDate)
                </td>
                <td>
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts{
    <script>
        $(document).ready(function () {
            //get the current date
            var currentDate = new Date();
            var formattedCurrentDate = currentDate.getMonth() + 1 + '/' + currentDate.getDate() + '/' + currentDate.getFullYear();

            // iterate over each row in the table
            $('#table_id tbody tr').each(function () {
                // get the closure date and final closure date of each row
                var closureDate = new Date($(this).find('td:eq(1)').text());
                var formattedClosureDate = closureDate.getMonth() + 1 + '/' + closureDate.getDate() + '/' + closureDate.getFullYear();
                var finalClosureDate = new Date($(this).find('td:eq(2)').text());
                var formattedFinalClosureDate = finalClosureDate.getMonth() + 1 + '/' + finalClosureDate.getDate() + '/' + finalClosureDate.getFullYear();

                // compare the dates and highlight the row based on the result
                if (formattedClosureDate > formattedCurrentDate) {
                    $(this).addClass('table-success');
                } else if (formattedFinalClosureDate < formattedCurrentDate) {
                    $(this).addClass('table-danger');
                } else $(this).addClass('table-warning');
            });
        })
    </script>
}