@using Microsoft.AspNetCore.Identity

@model IEnumerable<IdentityRole>

@{
	ViewData["Title"] = "Application Roles";
}

<h1> View Roles</h1>

<p>
	<a class="btn btn-dark" asp-action="Create New"> Create</a>
</p>

<table class="table">
	<thead>
		<tr>
			<th>Id</th>
			<th>Name</th>
			<th>Action</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Id</td>
				<td>@item.Name</td>
				<td>
					<a asp-action="Edit" asp-controller="Roles" class="btn btn-primary" asp-route-id="@item.Id">Edit</a>
					<a asp-action="Delete" asp-controller="Roles" class="btn btn-danger" asp-route-id="@item.Id">Delete</a>
				</td>
			</tr>

		}
	</tbody>
</table>
